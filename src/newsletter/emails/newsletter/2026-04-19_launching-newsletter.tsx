import { NewsletterEmailLoader } from "@/newsletter/components/newsletter-email-loader";

const LaunchingNewsletter = () => (
  <NewsletterEmailLoader slug="launching-newsletter" />
);

LaunchingNewsletter.title =
  "Build Your Own Newsletter — Talk Resources & Links";

export default LaunchingNewsletter;
